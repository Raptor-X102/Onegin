#ifndef FIND_STR_AMOUNT
#define FIND_STR_AMOUNT
#include <ctype.h>
#include <stdio.h>
#include "DEBUG_PRINTF.h"

unsigned long long int find_str_amount(char* buffer, const unsigned long long int size);

#endif
